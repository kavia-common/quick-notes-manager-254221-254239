# Copy to .env and fill in your values

# Supabase (optional). If missing, the app will use localStorage.
REACT_APP_SUPABASE_URL=
REACT_APP_SUPABASE_KEY=

# Optional environment label in UI
REACT_APP_NODE_ENV=development
